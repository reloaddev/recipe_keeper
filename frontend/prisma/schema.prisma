generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model recipe {
  title        String
  url          String
  ingredients  Json   @db.Json
  instructions String

  @@id([title, url])
}
